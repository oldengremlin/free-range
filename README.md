# free-range
A Ruby script to analyze VLAN distribution on network devices, generating tables or PNG images.

 sudo apt install imagemagick libmagickcore-dev libmagickwand-dev
 sudo gem install rmagick
